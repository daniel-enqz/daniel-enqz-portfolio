<%= turbo_frame_tag "flash_messages" do %>
  <% flash.each do |type, values| %>
      <%= render partial: "shared/flashes/#{type}", locals: { message: values[:message], body: values[:body] } %>
  <% end %>
<% end %>
